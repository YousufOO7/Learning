import React from 'react';
import PropTypes from 'prop-types';

const Graf = props => {
    return (
        <div>
            this is graf section
        </div>
    );
};

Graf.propTypes = {
    
};

export default Graf;