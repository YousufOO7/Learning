import PropTypes from 'prop-types';

const Header = props => {
    return (
        <div>
            <h2 className="text-2xl">Welcome Yousuf</h2>
        </div>
    );
};

Header.propTypes = {
    
};

export default Header;